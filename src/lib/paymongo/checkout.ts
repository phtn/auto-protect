// "use server";

// import type { CheckoutParams } from "@re-up/paymongo-fn/src/schema/zod.checkout";
// import { api } from "trpc/server";

// export const createCheckout = async (params: CheckoutParams) =>
//   await api.paymongo.createCheckout(params);
